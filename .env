# ============================================
# 所持品管理API - 環境変数設定テンプレート
# ============================================

# ------------------------------------------
# サーバー設定
# ------------------------------------------
# アプリケーションのポート番号（デフォルト: 8080）
PORT=:8080

# ------------------------------------------
# データベース設定 (MySQL)
# ------------------------------------------
# データベースホスト
# Docker環境: mysql (docker-compose.ymlのサービス名)
# ローカル環境: localhost
DB_HOST=mysql

# データベースポート
DB_PORT=3306

# データベースユーザー名
DB_USER=root

# データベースパスワード
DB_PASSWORD=password

# データベース名
DB_NAME=items_db

# ------------------------------------------
# 環境設定
# ------------------------------------------
# 実行環境 (development / staging / production)
APP_ENV=development

# ログレベル (debug / info / warn / error)
LOG_LEVEL=debug

# ------------------------------------------
# 設定ファイル使用方法
# ------------------------------------------
# 1. このファイルをコピーして .env を作成:
#    cp .env.example .env
#
# 2. 必要に応じて値を編集
#
# 3. Docker環境で起動:
#    docker-compose up -d --build
#
# 4. ローカル環境で起動する場合:
#    DB_HOST=localhost に変更してください